#!/bin/bash
# deepseek — wrapper: najde python s fridou a spusti cli/deepseek.py
set -u
HERE="$(cd "$(dirname "$0")" && pwd)"
ROOT="$(cd "$HERE/.." && pwd)"
. "$ROOT/../scripts/_python.sh"
PY="$(frida_mcp_python "$ROOT/..")"
exec "$PY" "$ROOT/cli/deepseek.py" "$@"
